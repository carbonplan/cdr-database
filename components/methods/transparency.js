import { Text, Link } from 'theme-ui'

const Transparency = () => {
  return (
    <>
    <Text sx={{ mb: [2] }}>
      Project Transparency assesses the degree to which projects
      make enough information available in their public proposals to
      enable independent evaluation.
    </Text>
    <Text>
      The availability of public proposals, which has allowed us to build
      this database, is itself highly transparent, so these values
      should be considered relative above and beyond that baseline.
    </Text>
    </>
  )
}

export default Transparency
